<%= form_tag("/matches/#{:match_id}/teams/#{:id}", method: "get") do %>
  <%= label_tag(:q, "Search for:") %>
  <%= text_field_tag(:q) %>
  <%= submit_tag("Search") %>
<% end %>

<% if @heroes.nil? == false %>
  <% @heroes.each do |hero| %>
    <%= hero.name %><br>
  <%end%>
<%end%>
